The code hve been tested and the output should work, however, you might encounter typographical errors in the comments or string characters, or literal. Code subject to changes as per lesson

The 2nd prog illustrate with us on how we can do pointer arithmatic and iterate through the array addresses of individual 
items stored in the array.

The first three lessons takes the reader on a tour exploring what pointers are, and sort gives the primer to a novice programmer on  what pointers are, pointer manipulation and there relationship with arrays.
Pointers are a powerful construct in c language and are therefore crucial to the language, pointers offers as with powerful tools in as much as memory managment is regarded. And pointers have proved so efficient when we are dealing with memory.